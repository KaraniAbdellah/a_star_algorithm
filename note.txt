Creating a Window --> Done
    Use tk.Tk() to initialize the main window.
    Use mainloop() to keep the application running.

Basic Tkinter Widgets
    Label (display text)
    Button (trigger actions)
    Entry (take user input)

Grid Layout (Optional UI Elements)
    Use pack(), grid(), or place() to arrange widgets.

Event Handling (User Interaction)
    Use bind() to detect clicks for selecting nodes.
    Use Button widgets to trigger A* execution.

Canvas for Drawing Graphs
    Use Canvas to draw nodes and edges.
    Use create_oval() for nodes and create_line() for edges.

Project Structure:
    use class diagramm
















////////////////////////////
Dialog boxes:
    Use messagebox.showinfo() for messages
    Use simpledialog.askstring() to get text input

Drawing features:
    Use canvas.delete() to remove drawings
    Use canvas.itemconfig() to change colors
    Use canvas.move() to move objects

Mouse actions:
    Use canvas.bind("<Button-3>") for right-click
    Use canvas.bind("<B1-Motion>") for dragging


